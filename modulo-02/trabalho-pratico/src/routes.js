import { Router } from 'express';

// import StateController from './app/controllers/StateController';

const routes = new Router();

// routes.post('/account', StateController.process);

export default routes;
